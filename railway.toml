[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 600
restartPolicyType = "always"
startCommand = "sh -c 'uvicorn server:app --host 0.0.0.0 --port ${PORT:-8080} --timeout-keep-alive 75'"
